/**
 * An interface to tell if an object is initialized and ready.<br>
 * May be used in combination with {@link Component} to tell if a component
 * is ready to render without showing a loading placeholder.
 * @interface Readyable
 */

/**
 * Returns a promise to when the object is initialized and ready.
 * @function
 * @name Readyable#ready
 * @returns {Promise} A promise to when the object is initialized and ready.
 */
